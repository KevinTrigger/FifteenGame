

function Square({value}) {

  return(
    <div className="w-full h-full bg-red-400 border border-black flex justify-center items-center font-bold text-3xl text-sky-100 hover:cursor-pointer rounded-md">{value}</div>
  )
}

export default Square;
